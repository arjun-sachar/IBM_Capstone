export default {
  showAPIKey: true, // generate API for users
  showAlerts: true, // show alerts in top header
  showUploads: true, // show uploads in dropdown on top right
  navBar: "left", // where navbar exists
  globalFilters: true // show global filters
};
